export interface EditCareer {
    workStart: { month: number, year: number };
    workEnd: { month: number, year: number };
    customer: string;
    business: string;
    oS: string;
    language: string;
    database: string;
    environment: string;
    workProcess: string;
    workDetail: string;
    position: string;
    memberNum: string;
  }